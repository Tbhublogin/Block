# Phase 3.1 — Populate Iraq (Civilization #1) Data + Localization

## Approved decisions (from your prompt + answers)
- Target-score curve: **linear, target = orderIndex × 1000** → 1000, 2000, … 30000 (stage ids `iraq_01`…`iraq_30`).
- Civilization id: **`iraq`** with a small refactor of `progress_controller.dart` to derive first/next civilization from the catalog instead of the `civilization_N` string convention.
- Landmarks: the 10 provided entries with exact landmarkIds and `assets/civilizations/iraq/<landmarkId>.png` paths (images themselves are attached in prompt 3.2; path strings are written now).

## 1. Implement the two missing models (pure Dart, no Flutter imports, hand-written ==/hashCode per codebase convention)
- `lib/domain/models/civilization.dart`: `Civilization { id, nameKey, emblemAsset (String?), themeColorHex, stages (List<Stage>) }` — const-constructible, replacing the stub, per PRD §11 (emblemAsset null-by-design for Iraq).
- New `lib/domain/models/landmark_info.dart`: `LandmarkInfo { id, civilizationId, nameKey, historicalFactKey, imageAsset }` — per PRD §11 (there is no existing file; PRD §4 does not name one, so this path is chosen).

## 2. Populate `lib/data/datasources/civilizations_data.dart`
- Doc comment + a `Civilization civilizationsCatalog`-style export: the **iraq** entry (`nameKey: civIraqName`, `emblemAsset: null`, `themeColorHex` from the existing gold/blue palette) + 30 `Stage` entries (`iraq_01`…`iraq_30`, orderIndex 1..30, targetScore = orderIndex × 1000) + 10 `LandmarkInfo` entries using your exact landmarkIds (`hanging_gardens`, `ziggurat_of_ur`, `tower_of_babel`, `hammurabi_stele`, `ishtar_gate`, `lamassu`, `golden_lyre_of_ur`, `naram_sin_stele`, `cuneiform_tablet`, `lion_of_babylon`), imageAsset `assets/civilizations/iraq/<id>.png`.

## 3. Localization — add 22 keys to both ARBs (Arabic template first, then regenerate)
- `civIraqName` = "الحضارة العراقية (بلاد الرافدين)" / "Iraq (Mesopotamia)".
- 10 name keys + 10 fact keys (prefix `landmark<Name>` / `landmark<Name>Fact`, camelCase): Arabic verbatim from your prompt; English translated faithfully — names use standard English forms (Hanging Gardens of Babylon, Great Ziggurat of Ur, Tower of Babel, Code of Hammurabi stele, Ishtar Gate, Assyrian Lamassu, Golden Lyre of Ur, Victory Stele of Naram-Sin, cuneiform clay tablet, Lion of Babylon); facts translated naturally without inventing anything beyond the Arabic (including the academic-caution wording for the Hanging Gardens).
- Run `flutter gen-l10n` and commit regenerated `lib/l10n/generated/` files.

## 4. Refactor `progress_controller.dart` to be catalog-driven (small)
- `firstCivilizationId` is replaced by catalog lookup: first civilization = the catalog's entry (its stage-1 unlock rule and Hive compatibility are unchanged — still `Map<String, int>` keyed by civilization id).
- `_nextCivilizationId` becomes the catalog's next entry after the current one (falls back to no-unlock for an unknown/last id).
- Export the current catalog id from the data file (single import), and update `test/application/progress_controller_test.dart` fixtures (`civilization_1/2/3` → catalog ids).

## 5. Update PROJECT_STATE.md
- Mark prompt 3.1 / the target-score curve as resolved (record the linear 1000→30000 decision), note the `iraq` id + catalog-driven progress refactor, update phase/content-status sections per the file's own instruction.

## Explicitly out of scope (later prompts)
- Copying PNGs / pubspec assets registration (prompt 3.2), `civilization_palettes.dart` filling, screens (Phase 6), the CustomPainter emblem (Phase 6 map screen).

## Verification
`flutter gen-l10n` → `flutter analyze` (0 issues) → `flutter test` (all pass, incl. refactored progress tests) → `dart format` clean.