# Prompt 3.2 — Attach Iraq landmark PNGs + raw-PNG vs CustomPainter decision

## Context established (from inspection)
- Your message was the "image attachments too large" error — no re-attach needed: the 10 final PNGs are already on disk and committed at `Pictures/<landmarkId>.png` with the exact target filenames (1254×1254, 1.4–2.6 MB each). Prompt 3.1 (Iraq data + localization) is already committed (S4): catalog, `Civilization`/`LandmarkInfo` models, ARB keys, 95 passing tests.
- **Flagged anomaly:** the git index holds a staged *rollback* of `PROJECT_STATE.md` to its pre-3.1 state, contradicting the committed code (likely debris from the crashed session). Step 1 discards it so doc updates build on reality — flag me if you staged it deliberately.
- File-type finding: 9/10 PNGs are RGB **without alpha** (1 is RGBA) — the art is full-bleed square paintings with baked backgrounds; transparency isn't actually present or needed for square-cell rendering. This shapes the A/B decision below.

## Steps
1. **Repo hygiene** — restore `PROJECT_STATE.md` to the committed (3.1) state (`git restore --staged` + `git checkout --`), discarding the stale staged revert.
2. **Inspect all 10 artworks cheaply** — generate ~320 px JPEG thumbnails into `%TEMP%` via Windows PowerShell (System.Drawing; no new packages), then view all 10 and judge: silhouette distinctiveness, per-landmark color identity, legibility at ~40–60 dp board-cell size, set consistency.
3. **The 3.2 decision layer** — form one group recommendation: **A)** use the PNGs directly for board/tray + museum, or **B)** full-quality PNGs for the museum only + hand-drawn simplified `CustomPainter` icons for the small board/tray tiles. Present findings and **confirm with you via AskUserQuestion before recording it** (PROJECT_STATE requires explicit confirmation). Museum uses full-quality PNGs either way.
4. **Wire assets** (prompt 3.2 body): copy the 10 PNGs verbatim into `assets/civilizations/iraq/` (no editing/renaming — names already match `civilizations_data.dart`); register the folder in `pubspec.yaml` `flutter: assets:`; populate the `core/constants/asset_paths.dart` TODO stub as the path registry and point `civilizations_data.dart`'s `imageAsset` values at it (data values unchanged → existing tests hold). The 6 Arabic-named UI mockups stay in `Pictures/` (Phase 6 reference, not assets); the Iraq emblem stays painter-drawn (no asset).
5. **Update PROJECT_STATE.md** — mark 3.2 complete; record the confirmed A/B decision + rationale + the no-alpha finding (Summary / §3 open decision resolved / §5 content status / Decision Log); next steps stay: theme 0.3 + minimal game screen (no landmark art needed yet).
6. **Verify** — `flutter pub get` → `dart format` → `flutter analyze` (0 issues) → `flutter test` (95 pass) → `flutter build apk --debug`.
7. **Commit as `S5`** (matching your S2/S3/S4 session-commit convention), including the step-1 doc restore.

## Flags (no action unless you say so)
- Raw assets total ≈ 18.7 MB → APK grows by that; prompt 3.2 forbids altering images so they go in verbatim. If APK size matters later, downscaled board-only variants can be added as an explicit exception.
- Under option A the board looks like a patchwork of square painting tiles (baked backgrounds visible); under B it's flat hand-drawn icons. Final visual call after step 2.