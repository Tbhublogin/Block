library;

/// 8x8 grid state, placement validation (`canPlace`/`place`), and
/// row/column clearing — pure Dart, fully unit-testable.
///
// TODO: implement BoardEngine in Phase 1/2 (BUILD_PROMPTS 1.2), including
// the special piece's unconditional row+column clear (PRD 5.2).
